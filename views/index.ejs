<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="style.css">
    <title>quora-platfrom</title>
</head>

<body>
    <h1>Quora ~ Post</h1>
 
    <% for(post of posts){ %>  
        <div class="post">
          <h3>@ <%= post.username %></h3>
          <p><%= post.content %></p>
          <a href="http://localhost:3000/post/<%= post.id%>">see post details</a>
          <a href="http://localhost:3000/post/<%= post.id%>/edit">update post</a>
          
          <form method="post" action="http://localhost:3000/post/<%= post.id%>?_method=delete">
            <button>delete Post</button>
          </form>
        </div>
 
    <% }%>
    <a href="http://localhost:3000/post/new">create new post</a>
    


</body>

</html>